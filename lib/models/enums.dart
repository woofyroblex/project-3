enum UserRole { user, admin }

enum VerificationStatus { unverified, pending, verified }

enum TransactionStatus { pending, completed, failed, cancelled }

enum TransactionType { finderFee, servicePayment, refund }
